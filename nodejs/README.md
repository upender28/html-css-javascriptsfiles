# Node.js Backend Practicals

These programs are included because the supplied experiments list also contains a Node.js Backend section. Unlike the browser HTML/CSS/JavaScript experiments, these files are run with Node.js from a terminal using `node filename.js` and some Express/database/authentication experiments require npm packages and additional setup.

Each file begins with Aim, Description, Steps, and Expected Output comments so it can be used as a practical record.
